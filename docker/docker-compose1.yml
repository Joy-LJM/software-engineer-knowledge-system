services:
  web:
    build: .
    ports:
      - "9000:5000"
    env_file:
      - .env
    depends_on:
      - redis
  redis:
    image: redis
    volumes:
      - redis-data:/data
volumes:
  redis-data: