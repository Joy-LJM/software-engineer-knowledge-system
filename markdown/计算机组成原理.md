# 计算机组成原理

- 二进制是以2为基数代表系统。这一系统中，通常用两个不同的符号0（代表零）和1（代表一）来表示，机器码由0和1组成；逢二进一

```纯文本 
二进制转10进制：1011为10进制的11
 8 4 2 1 
1 0 1 1=1*8+0*4+1*2+1*1=11
```


- 八进制：以数字0开始表明该数字是八进制，八进制数取值为0\~7

```纯文本 
017=1*8^1+7*8^0=8+7=5
011=1*8^1+1*8^0=8+1=9
```


- 汇编语言由编译器（提前转换）转换成机器码
- 解释器：把代码转成可执行机器码，运行时转换
- 队列queue：先进先出；入队，出队
- 栈stack：后进后出；入栈，出栈
- 文件在底层都是一长串的二进制
- 操作系统（OS）：也是程序，有操作硬件的特殊权限，可以运行和管理其他程序。一般是开机第一个启动的程序，其他程序由它启动。具有多任务处理，虚拟内存，动态内存分配（对程序来说，内存是连续的），分时操作系统（多用户）等特点
- 随机存取存储器（ RAM）：是与[CPU](https://baike.baidu.com/item/CPU?fromModule=lemma_inlink "CPU")直接交换数据的内部存储器。它可以随时读写（刷新时除外），而且速度很快，通常作为[操作系统](https://baike.baidu.com/item/操作系统?fromModule=lemma_inlink "操作系统")或其他正在运行中的程序的临时数据存储介质。RAM工作时可以随时从任何一个指定的地址写入（存入）或读出（取出）信息。只有在有电的情况下存储东西
- 内存：持久存储，电源关闭时数据也不会丢失
- 寄存器（register）：能存一个数字，这个数字有多少位，叫位宽
- 中央处理器（CPU）：执行程序，过程：取指令→解码→执行
- 缓存（cache）：当临时空间，存一些中间值
- 脏位：缓存的数据与RAM数据不一致
- 1字节byte=8位bite

![](image/image_hUap3OYnuG.png)

- 局域网（LAN）

- 广域网（WAN）：其路由器一般属于你的互联网服务提供商（ISP）

- MAC地址：media access control address, 媒体访问控制地址

- 带宽：固定时间可传输的资料数量；单位：bps;

- 路由routing：一个点到一个点的路线；

- 数据包：大报文分成小块，每个数据包都有目标地址

- IP：负责把数据包送到正确的计算机

- UDP：用户数据报协议，负责把数据包送到正确的程序；不提供数据修复或数据重发机制；无法知道数据包是否到达；速度快，简单；

- TCP/IP：功能1：数据包有序号，可以解决乱序问题（数据包走的路由不一样，到达顺序不一样）；功能2：TCP要求接收方的电脑收到数据包并且checksum(检验和)检查无误后（数据没有丢失）给发送方发一个ACK确认码，代表收到了；如果过一定时间没有收到，发送方就重发；可以同时发送多个数据包；可以根据拥挤情况自动调整传输率

- DNS服务器：负责把域名和IP地址一一对应；数据为树状结构；分为顶级域名，二级域名，子域名

- [Layer of OSI model](https://www.geeksforgeeks.org/computer-networks/open-systems-interconnection-model-osi/ "Layer of OSI model")

  应用层Application Layer（UI:浏览器，APP, protocol:HTTP,DNS, FTP）

  表示层Represantation Layer(data formatted, TLS/SSL encryption and decryption)

  会话层Session Layer(connection established)

  传输层Transport Layer（segmentation;UDP，TCP, 负责计算机之间点到点的传输）

  网络层Network Layer（IP负责各种报文交换和路由）

  数据链路层Data Link Layer（node-to-node delivery, framing; MAC）

  物理层The physical layer(transmits raw bits over physical media, ex: cables)
  
- URL：统一资源定位符，每个网页唯一的地址

- HTTP：超文本传输协议，用于向服务器请求页面等资源

- 网络中立性：所有数据包应平等对待，速度和优先级一样

- 节流：故意给更少带宽和更低优先级

- 蠕虫（worm）：恶意程序在多台计算机间传播

- 僵尸网络：多台被恶意控制的计算机形成的网络

- DDOS：僵尸网络里的所有电脑同时发一大堆垃圾信息堵塞服务器

- 加密encryption：明文→密文

- 解密decryption：密文→明文

- 密钥交换：是一种不发送密钥，但依然让两台计算机在密钥上达成共识的算法

- 对称加密：双方用一样的密钥加密和解密消息（如AES）

- 非对称加密：RSA
# networking
- [HTTP 1.1 vs HTTP 2](https://www.cyberdefensemagazine.com/http-1-1-vs-http-2-what-are-the-differences/)
  -[more] （https://github.com/woai3c/Front-end-articles/blob/master/http-https-http2.md）